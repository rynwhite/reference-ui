/**
 * Stub: css() is provided by the consumer's generated output after running
 * the CLI. This exists so reference-core source type-checks; at runtime the
 * compiler materializes real styling. Returns a placeholder class.
 */
export function css(_styles: Record<string, unknown>): string {
  return 'ref-styles';
}
